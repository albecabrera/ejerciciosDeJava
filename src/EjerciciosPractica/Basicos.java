package EjerciciosPractica;

public class Basicos {
    public static void main(String[] args) {
        /*
         * JAVA ÜBUNGSPLAN – for, for-each, while, do-while, Scanner
         * ------------------------------------------------------------
         * Anleitung: Code für jede Aufgabe direkt unter den Kommentar schreiben.
         * Reihenfolge: leicht -> schwer, pro Thema.
         */

/*
                // ==========================================================
                // THEMA 1: for-Schleife
                // ==========================================================

                // --- Aufgabe 1.1 (leicht) ---
                // Gib die Zahlen von 1 bis 10 mit einer for-Schleife aus.
                for (int numero = 1; numero < 11; numero++) {
                    System.out.println("Los numeros son = " + numero);

                }
        System.out.println("Conteo de números finalizados");
        }

                // --- Aufgabe 1.2 (schwer) ---
                // Gib mit einer for-Schleife (oder verschachtelten for-Schleifen)
                // das Einmaleins (1x1 bis 10x10) als Tabelle aus.
                // Erwartetes Format z.B.:
                // 1  2  3  4  5 ...
                // 2  4  6  8 10 ...
                // ...

                */
                // Einmaleins

    }

                // ==========================================================
                // THEMA 2: erweiterte for-Schleife (for-each)
                // ==========================================================

                // --- Aufgabe 2.1 (leicht) ---
                // Gegeben ist ein int-Array. Gib mit einer for-each-Schleife
                // jedes Element einzeln aus.
                int[] zahlen = {4, 8, 15, 16, 23, 42};


                // --- Aufgabe 2.2 (schwer) ---
                // Gegeben ist ein Array mit Namen (String[]). Finde mit einer
                // for-each-Schleife den längsten Namen und gib ihn am Ende aus.
                // (Hinweis: keine normale for-Schleife mit Index verwenden.)
                String[] namen = {"Anna", "Bartholomäus", "Li", "Christopher", "Jo"};


                // ==========================================================
                // THEMA 3: while-Schleife
                // ==========================================================

                // --- Aufgabe 3.1 (leicht) ---
                // Zähle mit einer while-Schleife von 10 rückwärts bis 1 herunter
                // und gib danach "Start!" aus.


                // --- Aufgabe 3.2 (schwer) ---
                // Berechne mit einer while-Schleife die Quersumme einer Zahl,
                // bis nur noch eine einstellige Zahl übrig bleibt (iterative Quersumme).
                // Beispiel: 9875 -> 9+8+7+5=29 -> 2+9=11 -> 1+1=2
                int startZahl = 9875;


                // ==========================================================
                // THEMA 4: do-while-Schleife
                // ==========================================================

                // --- Aufgabe 4.1 (leicht) ---
                // Verwende eine do-while-Schleife, um die Zahlen 1 bis 5
                // auszugeben (die Schleife muss mindestens einmal laufen).


                // --- Aufgabe 4.2 (schwer) ---
                // Simuliere mit einer do-while-Schleife ein Ratespiel:
                // Eine Zielzahl ist fest im Code vorgegeben (z.B. 7).
                // Frage den Nutzer wiederholt (mit Scanner) nach einer Zahl,
                // bis er die richtige Zahl errät. Gib bei jedem Versuch
                // "zu hoch" oder "zu niedrig" aus.
                int zielZahl = 7;


                // ==========================================================
                // THEMA 5: Scanner (Nutzereingaben)
                // ==========================================================

                // --- Aufgabe 5.1 (leicht) ---
                // Lies mit Scanner den Namen des Nutzers ein und gib
                // "Hallo, <Name>!" aus.


                // --- Aufgabe 5.2 (schwer) ---
                // Lies mit Scanner so lange ganze Zahlen vom Nutzer ein,
                // bis er "0" eingibt. Summiere alle eingegebenen Zahlen
                // (außer der 0) und gib am Ende Summe und Durchschnitt aus.
                // Kombiniere dafür Scanner mit einer while- oder do-while-Schleife.


            }


