{
  "name": "java-werkstatt",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright smoke tests for Java Werkstatt",
  "scripts": {
    "test:syntax": "node --check game.js && node --check python.js && node --check playwright.config.js && node --check tools/xampp-smoke.mjs && node --check tools/cloud-workflow-smoke.mjs && php -l api/account.php && php -l api/assignments.php && php -l api/attempts.php && php -l api/auth.php && php -l api/bootstrap.php && php -l api/classes.php && php -l api/collaboration.php && php -l api/compile.php && php -l api/feedback.php && php -l api/notifications.php && php -l api/progress.php && php -l api/python.php && php -l api/submissions.php",
    "test:worker": "bash tools/test-sandbox-worker.sh",
    "test:xampp": "node tools/xampp-smoke.mjs",
    "test:cloud": "node tools/cloud-workflow-smoke.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "playwright:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
